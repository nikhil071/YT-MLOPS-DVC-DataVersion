1. create a git repo and clone it in local.
2. create a code.py and add mycode to it.( it wii save a csv file to a new "data" folder.)
3. DO a git add-commit-push before initializing dvc
#pip install dvc
4. now we do we "dvc init" (creates, .dvcgitignore, .dvc)
5. Now do "mkdir S3".(creates a new s3 directory)
6. Now we do "dvc remote add -d myremote S3"
7. Next "dvc add data/"
 Now it will ask to do (git rm -r --cached "data" and git commit -m "stop tracking data"")
8.Again we do "dvc add data/"(creates dvc.data and then "git add data.dvc .gitignore")
9.Now "dvc commit" and then  dvc "push" 
10. Do a git add-commmit-push to mark this stage as first version of data
11. Now make the changes to mycode.py to append a new row in data, check changes via "dvc status".
12. Again "dvc commit" and then "dvc push"
13. Then git-add-commit-push.(we are saving version to of our data at this project)
14. check dvc and git staus, everything should be up to date
15. Repeat same step for V3 of data